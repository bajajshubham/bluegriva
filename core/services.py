"""
Business logic for the core app.

core/views.py (HTMX) and, later, api/v1/ (JSON for Flutter) both call
functions from this file — never the other way around. Neither layer
touches core/models.py directly.

No functions yet — core only renders a placeholder page so far.
"""