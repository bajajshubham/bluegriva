"""
DRF serializers for the Flutter app.

One serializer class per model that needs to cross the API boundary.
Serializers stay thin — they shape data, they don't contain business logic.
Add the first serializer here once catalog (or another app) has a model.
"""